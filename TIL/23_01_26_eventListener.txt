# 스크롤 이벤트리스너 

window.scrollY
현재 페이지를 얼마나 위에서 부터 스크롤했는지 px 단위로 알려준다.

window.scrollX
가로로 얼마나 스크롤했는지 알려준다. (가로 스크롤바가 있으면)

window.scrollTo(x, y)
강제로 스크롤바를 움직일 수 있다.

window.scrollBy(x, y)
현재 위치에서부터 스크롤해준다.

# jQuery 버전
$(window).on('scroll', function(){
  $(window).scrollTop();
})